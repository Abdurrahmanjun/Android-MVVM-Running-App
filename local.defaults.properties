# Default secrets used when secrets.properties is absent.
# Replace GMP_KEY in a local secrets.properties (git-ignored) with a real
# Google Maps Platform API key to load the map at runtime.
GMP_KEY=YOUR_MAPS_API_KEY
